(()=>{'use strict';
const app=document.getElementById('app');let busy=false;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const qs=(s,r=document)=>r?.querySelector?.(s)||null;const qsa=(s,r=document)=>r?[...r.querySelectorAll(s)]:[];
function enhance(){
 if(busy||location.pathname!=='/espace')return;const dash=qs('.dashboard');if(!dash||dash.dataset.workspace==='1')return;
 const head=qs('.dashboard-head',dash),profile=qs('.profile-card',dash),grid=qs('.dashboard-grid',dash);
 if(!head||!profile||!grid)return;const list=qs('.panel:not(.profile-card)',grid);if(!list)return;
 busy=true;dash.dataset.workspace='1';
 const title=qs('h1',head)?.textContent||'Bonjour';const subtitle=qs('p',head)?.textContent||'';const owner=!!qs('.stats',dash);
 const stats=qsa('.stat',dash);const originalList=qs('.consultation-list',list);const originalProfileButtons=qsa('.profile-card button',dash);const originalBook=qs('#book',head);
 const listTitle=qs('.panel-head h2',list)?.textContent||'Mes consultations';const count=qs('.panel-head .form-note',list)?.textContent||'';
 const name=qs('.profile-card h3',dash)?.textContent||'';const email=qs('.profile-card p',dash)?.textContent||'';const role=qs('.profile-card .badge',dash)?.textContent||'Client';const avatar=qs('.avatar',dash)?.textContent||'LC';
 dash.innerHTML=`<div class="space-shell ${owner?'is-owner':'is-client'}">
 <aside class="space-sidebar"><div class="space-brand"><div class="space-brand-icon">✦</div><div><strong>L'Art de la Connexion</strong><span>${owner?'Espace professionnel':'Espace personnel'}</span></div></div><div class="space-user"><div class="space-avatar">${esc(avatar)}</div><div><strong>${esc(name)}</strong><span>${esc(role)}</span></div></div>
 <nav class="space-nav" aria-label="Navigation de l'espace"><button class="space-nav-item active" data-space-tab="home"><span>⌂</span> Vue d'ensemble</button><button class="space-nav-item" data-space-tab="consultations"><span>◷</span> ${owner?'Consultations':'Mes rendez-vous'}</button><button class="space-nav-item" data-space-tab="profile"><span>◉</span> Mon profil</button>${owner?'<a class="space-nav-item" href="/admin" data-link><span>⌘</span> Administration</a>':''}</nav><div class="space-sidebar-bottom" id="spaceProfileActions"></div></aside>
 <main class="space-main"><header class="space-topbar"><div><span class="space-kicker">${owner?'TABLEAU DE BORD PROFESSIONNEL':'VOTRE ESPACE PRIVÉ'}</span><h1>${esc(title)}</h1><p>${esc(subtitle)}</p></div><div class="space-top-actions">${owner?'<a class="button button-small" href="/admin" data-link>Centre de contrôle →</a>':'<button class="button button-small" id="spaceBook">+ Nouvelle réservation</button>'}</div></header>
 ${owner?`<section class="space-kpis">${stats.map((s,i)=>`<article class="space-kpi"><span>${esc(qs('span',s)?.textContent||'Indicateur')}</span><strong>${esc(qs('strong',s)?.textContent||'—')}</strong><small>${['Votre activité','Prochaines séances','Revenus terminés'][i]||'En temps réel'}</small><i>${['↗','◷','€'][i]||'✦'}</i></article>`).join('')}</section>`:`<section class="space-welcome"><div><span class="space-kicker">BIENVENUE</span><h2>Tout votre parcours, au même endroit.</h2><p>Réservez une séance, retrouvez vos rendez-vous et rejoignez votre consultation depuis un espace simple et confidentiel.</p><div class="space-quick"><button class="button button-small" id="spaceBook">Réserver une consultation</button><button class="button button-small button-secondary" data-space-tab="consultations">Voir mes rendez-vous</button></div></div><div class="space-orbit"><span>✦</span><b>Confidentiel</b><small>Votre espace privé</small></div></section>`}
 <section class="space-content" data-space-panel="home"><div class="space-section-head"><div><span class="space-kicker">${owner?'ACTIVITÉ RÉCENTE':'VOS RENDEZ-VOUS'}</span><h2>${esc(listTitle)}</h2></div><span class="space-count">${esc(count)}</span></div><div class="space-list-wrap" data-list-target></div></section>
 <section class="space-content is-hidden" data-space-panel="consultations"><div class="space-section-head"><div><span class="space-kicker">AGENDA</span><h2>${owner?'Toutes les consultations':'Mes rendez-vous'}</h2></div></div><div class="space-list-wrap" data-list-target></div></section>
 <section class="space-content is-hidden" data-space-panel="profile"><div class="space-section-head"><div><span class="space-kicker">VOS INFORMATIONS</span><h2>Mon profil</h2></div></div><div class="space-profile-card"><div class="space-avatar large">${esc(avatar)}</div><div><h3>${esc(name)}</h3><p>${esc(email)}</p><span class="space-role">${esc(role)}</span></div><div class="space-profile-action" id="spaceProfileMain"></div></div></section></main></div>`;
 const moveList=tab=>{const target=qs(`[data-space-panel="${tab}"] [data-list-target]`,dash);if(target&&originalList)target.appendChild(originalList)};moveList('home');
 originalProfileButtons.forEach(b=>qs('#spaceProfileActions',dash)?.appendChild(b));const edit=originalProfileButtons.find(b=>/profil/i.test(b.textContent));if(edit)qs('#spaceProfileMain',dash)?.appendChild(edit.cloneNode(true));
 const book=qs('#spaceBook',dash);if(book&&originalBook)book.onclick=()=>originalBook.click();
 qsa('[data-space-tab]',dash).forEach(b=>b.addEventListener('click',e=>{const tab=e.currentTarget.dataset.spaceTab;if(!tab)return;if(tab==='home'||tab==='consultations')moveList(tab);qsa('[data-space-panel]',dash).forEach(p=>p.classList.toggle('is-hidden',p.dataset.spacePanel!==tab));qsa('.space-nav-item[data-space-tab]',dash).forEach(x=>x.classList.toggle('active',x.dataset.spaceTab===tab));}));
 busy=false;
}
const observerTarget=app||document.body;new MutationObserver(()=>requestAnimationFrame(enhance)).observe(observerTarget,{childList:true,subtree:true});setTimeout(enhance,300);
})();